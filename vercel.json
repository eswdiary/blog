{
  "builds": [
    {
      "src": "./public/**",
      "use": "@vercel/static"
    }
  ],
  "installCommand": "curl -fsSL https://github.com/getzola/zola/releases/download/v0.17.2/zola-v0.17.2-x86_64-unknown-linux-gnu.tar.gz | tar -xzf - -C /tmp && mv /tmp/zola /usr/local/bin/ && git submodule update --init --recursive",
  "buildCommand": "zola build"
}
